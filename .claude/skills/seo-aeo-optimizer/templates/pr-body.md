# 🔍 SEO/AEO Optimization Run — {{DATE}}

## Score Summary

| Metric | Before | After | Delta |
|---|---|---|---|
| **Average page score** | {{BEFORE_TOTAL}} / 100 | {{AFTER_TOTAL}} / 100 | {{DELTA}} |

Target thresholds: ≥95 to merge as a clean win, ≥99 to be considered "best in class."

## Changes Made

This PR groups changes by category. The risk label below identifies which category dominates the diff.

- [ ] Metadata (title, description, canonical, OG, Twitter)
- [ ] Sitemap updated
- [ ] Robots.txt updated
- [ ] Structured data (JSON-LD) added or fixed
- [ ] AEO answer blocks added
- [ ] FAQ schema added (around real, visible Q&A)
- [ ] Internal links improved
- [ ] Image alt text added
- [ ] Content quality improvements (non-revenue/legal pages only)
- [ ] Core Web Vitals fixes

## Validation Results

{{VALIDATIONS}}

## GitHub Repo Metadata Checklist (Phase 9)

Per `references/github-repo-optimization.md`. This is a binary checklist — repo metadata is per-repo (not per-page) so it does not roll into the 100-point page-score rubric above. Public repos only; skipped for private repos.

- [ ] Description populated (≤ 350 chars, sentence form, keyword-rich)
- [ ] ≥ 5 topics including both tech and domain terms
- [ ] README has H1, one-line description, **live-app link in first 3 lines**, "What it does", "Who it's for", "Tech stack", LICENSE link
- [ ] CNAME present and correct (GitHub Pages deploys only — skip otherwise)
- [ ] `LICENSE` file present (repos without one are not legally reusable)

If this PR is being run against a private repo, mark all items N/A and continue.

## 📝 Manual Tasks (cannot be auto-applied)

These items the skill suggests but does not apply on its behalf — either because they live outside the codebase (GitHub repo settings, third-party dashboards) or because they require human input the skill must not invent.

{{MANUAL_TASKS}}

Common manual-task entries the skill emits:

- **Set repo description** at `https://github.com/{{OWNER}}/{{REPO}}` → "About" gear icon. Suggested value: `{{SUGGESTED_DESCRIPTION}}`
- **Set repo topics** at the same panel. Suggested topics: `{{SUGGESTED_TOPICS}}`
- **Replace `REPLACE_WITH_GSC_TOKEN`** in the verification meta tag. Get it at https://search.google.com/search-console → Add property → HTML tag.
- **Replace `REPLACE_WITH_BING_TOKEN`** in the verification meta tag. Get it at https://www.bing.com/webmasters → Add a site → Meta tag option. (Bing verification also unlocks visibility in ChatGPT Search and Microsoft Copilot, which both source from the Bing index.)
- **Submit sitemap to Google Search Console and Bing Webmaster Tools** after verification clears.

If this section reads `{{MANUAL_TASKS}}` literally, the skill found nothing requiring manual action.

## ⚠️ Human Review Required

The following changes MUST be reviewed by a human before merge:

{{HUMAN_REVIEW_ITEMS}}

If none, this section reads: _No high-risk changes in this run._

Per `~/.claude/skills/seo-aeo-optimizer/references/safety-guardrails.md`, the categories that always require review:

- Homepage hero / positioning copy
- Pricing / plan / billing copy
- Legal pages (Terms, Privacy, Cookie)
- Awards content (especially in `awardssubmission`)
- Testimonials, social proof, "Featured in" sections
- Compliance language (HIPAA, COPPA, GDPR, ADA)
- Casting / talent / payment / employment claims
- Anything involving children, financial advice, or medical advice

## Risk Level

**`risk: {{RISK_LABEL}}`**

| Label | What it means | Reviewer action |
|---|---|---|
| `risk: low` | Metadata, sitemap, robots, schema additions, alt text | Skim diffs; merge if validators green |
| `risk: medium` | Content rewrites in non-sensitive pages, FAQ additions | Read content changes; confirm voice/accuracy |
| `risk: high` | Touches the categories listed above | Full review by Andrew before merge |

## Audit Log

This run has been appended to `docs/seo/audit-history.jsonl` for trend analysis. Future runs will compare against the latest entry to detect regressions.

## How to test locally

```bash
git fetch origin
git checkout {{BRANCH}}  # filled by create-optimization-pr.ts
npm install
npm run seo:validate
npm run seo:lighthouse
```

## Rollback

If anything is wrong, revert this PR. Metadata/sitemap/robots changes are immediately reversible. Schema additions don't break anything if removed. Lighthouse-driven perf changes (if any code changed) need careful rollback.

## Generated by

🤖 `seo-aeo-optimizer` skill (v1.0.0) via `~/.claude/skills/seo-aeo-optimizer/scripts/create-optimization-pr.ts`
